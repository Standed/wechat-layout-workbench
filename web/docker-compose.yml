services:
  wechat-layout-workbench:
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /app
    command: python web/server.py 8765
    environment:
      HOST: 0.0.0.0
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
    ports:
      - "8765:8765"
    volumes:
      - ..:/app
